import React from 'react';
import PictureCard from '@components/PictureCard';
import pic from '@images/pic';

const App = () => {
  return (
    <div className="app">
      <PictureCard src={pic} text="From App" />
      <PictureCard src={pic} text="From App" />
      <PictureCard src={pic} text="From App" />
      <PictureCard src={pic} text="From App" />
      <PictureCard src={pic} text="From App" />
      <PictureCard src={pic} text="From App" />
      <PictureCard src={pic} text="From App" />
      <PictureCard src={pic} text="From App" />
      <PictureCard src={pic} text="From App" />
      <PictureCard src={pic} text="From App" />
    </div>
  );
};

export default App;
